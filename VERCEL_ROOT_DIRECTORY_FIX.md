# 🔴 المشكلة: Root Directory فارغ!

## ⚠️ المشكلة الحالية:
من الصورة، أرى أن **Root Directory** في Dashboard **فارغ** (empty).

هذا يسبب الخطأ:
```
npm error path /vercel/path0/package.json
```

## ✅ الحل (خطوة بخطوة):

### 1. افتح إعدادات Build and Deployment:
https://vercel.com/dasme-projects/dasm-realty/settings/build-and-deployment

### 2. ابحث عن قسم "Root Directory":
- يجب أن يكون في الصفحة تحت قسم "Build and Development Commands"

### 3. في حقل "Root Directory":
- **اكتب:** `frontend` (بدون علامات اقتباس)
- **لا تتركه فارغاً!**

### 4. اضغط "Save":
- يجب أن يصبح الزر أزرق (غير رمادي)
- اضغط عليه لحفظ التغييرات

### 5. بعد الحفظ:
- اذهب إلى تبويب **"Deployments"**
- اضغط على آخر deployment
- اضغط **"Redeploy"** → **"Redeploy"** مرة أخرى

---

## 📸 كيف يجب أن تبدو الإعدادات:

```
Root Directory: [frontend]  ← يجب أن يكون مكتوب هنا
```

**وليس:**
```
Root Directory: []  ← فارغ ❌
```

---

## 🔍 التحقق من أن الإعدادات صحيحة:

بعد ضبط Root Directory = `frontend` وحفظ التغييرات، يجب أن ترى في logs:
```
Running "install" command: `npm install`...
> Found package.json in frontend/package.json
```

**وليس:**
```
npm error path /vercel/path0/package.json  ← هذا يعني Root Directory فارغ
```

---

## ⚡ إذا لم يظهر زر Save:

1. تأكد من أنك غيرت قيمة Root Directory من فارغ إلى `frontend`
2. إذا كان الزر رمادي، جرب:
   - تحديث الصفحة (F5)
   - الخروج والدخول مرة أخرى
   - التأكد من أن لديك صلاحيات تعديل المشروع

---

**الرابط المباشر:**
https://vercel.com/dasme-projects/dasm-realty/settings/build-and-deployment
